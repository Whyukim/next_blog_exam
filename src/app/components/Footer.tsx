interface IFooter {}

function Footer({}: IFooter) {
  return (
    <footer className="flex items-center justify-center w-full bg-slate-500 h-[40px]  max-w-[1220px] mx-auto">
      footer
    </footer>
  );
}

export default Footer;
